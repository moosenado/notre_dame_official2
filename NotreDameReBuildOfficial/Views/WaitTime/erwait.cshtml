<div id="erwait">
    @model NotreDameReBuildOfficial.Models.erwaitClass

    ER Wait: @if (Model.getWaitTime() != null)
    {
        @Model.getWaitTime()
        
    }
    else
    {
        <span>Updating...</span>
    }
    and @Model.waitListCount() people waiting
</div>